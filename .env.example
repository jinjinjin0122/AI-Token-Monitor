# Use one or both. FMP is recommended because it returns quote + valuation metrics in a simple shape.
FMP_API_KEY=your_financial_modeling_prep_key
FINNHUB_API_KEY=your_finnhub_key

# Optional. Used by UI to display update policy.
NEXT_PUBLIC_REFRESH_SECONDS=60
